// Definición de la paleta corporativa y extensiones para el CDN
tailwind.config = {
    theme: {
        extend: {
            colors: {
                brand: '#00B4CE',   // Celeste corporativo
                dark: '#00597C',    // Azul principal
                accent: '#B4BD10',  // Verde de realce
                plomo: '#3C3E3F', // Plomo
                gris: '#9D9D9C', // Gris
            },
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            }
        }
    }
}